<% provide(:title, @title) %>
<div class="row">
  <div class="col-md-12 pagename">
    <h3 class="pagetitle">ユーザー一覧</h3>
  </div>
  <div class="search_form">
    <%= render'users/search_form' %>
  </div>
</div>

<%= will_paginate %>

<% unless @users.empty? %>
  <ul class="users">
    <%= render @users %>
  </ul>
<% else %>
  <p>
    ユーザーを見つけることができませんでした。<br>
    キーワードの再確認をお願いします。
  </p>
<% end %>

<%= will_paginate %>
