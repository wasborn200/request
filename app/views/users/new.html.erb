<% provide(:title, '新規アカウント作成') %>
<% provide(:url, signup_path) %>
<% provide(:button_text, 'アカウントを作成する') %>
<div class="row">
  <div class="col-md-12 pagename">
    <h3 class="pagetitle">新規アカウント作成</h3>
  </div>
<div class="row">
  <div class="col-sm-6 col-sm-offset-3">
    <%= render 'form' %>
  </div>
</div>
